# 🔥 نسيت كلمة السر - دليل سريع

## ✅ ما تم إضافته:

### 1. **زر "نسيت كلمة السر"** في شاشة تغيير كلمة المرور
- يظهر تحت حقل كلمة المرور الحالية
- أيقونة 📧 مع نص "نسيت كلمة السر؟"
- لون أحمر (#DC2626)

### 2. **Firebase Authentication**
- استخدام `@react-native-firebase/auth`
- إرسال بريد إلكتروني تلقائي
- رابط آمن لإعادة تعيين كلمة المرور

### 3. **رسائل واضحة**
- تأكيد قبل الإرسال
- رسالة نجاح مع تذكير بفحص البريد
- معالجة الأخطاء (بريد غير موجود، طلبات كثيرة، إلخ)

---

## 🚀 كيفية التثبيت:

### الطريقة السريعة:
```bash
./install-firebase.sh
```

### الطريقة اليدوية:
```bash
cd Q8SportApp
npm install @react-native-firebase/app @react-native-firebase/auth
```

---

## ⚙️ الإعداد المطلوب:

### 1. Firebase Console
1. اذهب إلى: https://console.firebase.google.com
2. أنشئ مشروع "Q8 Sport Car"
3. فعّل **Authentication** > **Email/Password**

### 2. قالب البريد الإلكتروني
في Firebase Console > Authentication > Templates > Password reset:

**الموضوع:**
```
إعادة تعيين كلمة المرور - Q8 Sport Car
```

**المحتوى:**
```
مرحباً،

تلقينا طلباً لإعادة تعيين كلمة المرور لحسابك في Q8 Sport Car.

اضغط على الرابط أدناه لإعادة تعيين كلمة المرور:
%LINK%

إذا لم تطلب إعادة تعيين كلمة المرور، يمكنك تجاهل هذا البريد.

مع تحياتنا،
فريق Q8 Sport Car
```

### 3. Android Setup
```bash
# حمّل google-services.json من Firebase Console
# ضعه في: Q8SportApp/android/app/google-services.json
```

في `android/build.gradle`:
```gradle
buildscript {
  dependencies {
    classpath 'com.google.gms:google-services:4.4.0'
  }
}
```

في `android/app/build.gradle`:
```gradle
apply plugin: 'com.google.gms.google-services'
```

### 4. iOS Setup
```bash
# حمّل GoogleService-Info.plist من Firebase Console
# ضعه في: Q8SportApp/ios/Q8SportApp/GoogleService-Info.plist

cd Q8SportApp/ios
pod install
cd ..
```

---

## 📱 كيفية الاستخدام:

### للمستخدم:
1. افتح التطبيق
2. اذهب إلى **حسابي** > **تغيير كلمة المرور**
3. اضغط على **📧 نسيت كلمة السر؟**
4. أكد إرسال البريد
5. افتح بريدك الإلكتروني
6. اضغط على الرابط
7. أدخل كلمة المرور الجديدة

### للمطور:
```javascript
// الكود موجود في:
// Q8SportApp/src/screens/Profile/ChangePasswordScreen.js

import auth from '@react-native-firebase/auth';

// إرسال بريد إعادة التعيين
await auth().sendPasswordResetEmail(user.email);
```

---

## 🔒 الأمان:

- ✅ الرابط صالح لمدة ساعة واحدة فقط
- ✅ يمكن استخدامه مرة واحدة فقط
- ✅ يتطلب تأكيد البريد الإلكتروني
- ✅ حماية من الطلبات الكثيرة (Rate Limiting)

---

## ⚠️ ملاحظات مهمة:

1. **يجب أن يكون البريد الإلكتروني مسجلاً في Firebase**
   - إذا كان المستخدم مسجل في قاعدة البيانات فقط، لن يعمل
   - يجب مزامنة المستخدمين مع Firebase

2. **فحص صندوق الرسائل غير المرغوب فيها**
   - قد يذهب البريد إلى Spam
   - ذكّر المستخدمين بفحصه

3. **التحقق من الإعدادات**
   - تأكد من تفعيل Email/Password في Firebase
   - تأكد من إضافة Domain في Authorized domains

---

## 🐛 حل المشاكل:

### "البريد الإلكتروني غير مسجل في Firebase"
```javascript
// يجب إنشاء المستخدم في Firebase عند التسجيل:
await auth().createUserWithEmailAndPassword(email, password);
```

### "لم يصل البريد"
1. تحقق من صندوق Spam
2. تحقق من إعدادات Firebase Console
3. تحقق من Authorized domains

### "خطأ في التثبيت"
```bash
# امسح node_modules وأعد التثبيت
cd Q8SportApp
rm -rf node_modules
npm install
cd ios && pod install && cd ..
```

---

## 📞 الدعم:

إذا واجهت أي مشكلة:
- 📧 summit_kw@hotmail.com
- 📱 راجع FIREBASE_SETUP.md للتفاصيل الكاملة

---

**تم التحديث:** الآن ✅
**الحالة:** جاهز للاستخدام بعد إعداد Firebase 🚀
