# ⚙️ دليل الإعداد السريع

## 1️⃣ إعداد Cloudinary (5 دقائق)

### الخطوات:
1. اذهب إلى: https://cloudinary.com/users/register/free
2. سجل حساب مجاني
3. بعد التسجيل، اذهب إلى Dashboard
4. انسخ البيانات التالية:
   - **Cloud Name**
   - **API Key**
   - **API Secret**

### أضف في ملف .env:
```env
CLOUDINARY_CLOUD_NAME="your_cloud_name_here"
CLOUDINARY_API_KEY="123456789012345"
CLOUDINARY_API_SECRET="abcdefghijklmnopqrstuvwxyz"
```

### اختبار:
```bash
# شغل المشروع
npm run dev

# ارفع صورة من الموقع
# يجب أن تظهر على Cloudinary Dashboard
```

---

## 2️⃣ إعداد Resend (5 دقائق)

### الخطوات:
1. اذهب إلى: https://resend.com/signup
2. سجل حساب مجاني
3. اذهب إلى API Keys
4. اضغط "Create API Key"
5. انسخ الـ API Key (يبدأ بـ `re_`)

### أضف في ملف .env:
```env
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxxx"
EMAIL_FROM="Q8Sport <noreply@q8sportcar.com>"
```

### اختبار:
```typescript
// في أي API route
import { sendWelcomeEmail } from '@/lib/email';

await sendWelcomeEmail('test@example.com', 'Test User');
```

---

## 3️⃣ إعداد Domain للبريد (اختياري)

### إذا كان لديك دومين:
1. في Resend Dashboard، اذهب إلى Domains
2. اضغط "Add Domain"
3. أدخل: `q8sportcar.com`
4. أضف DNS Records في إعدادات الدومين:
   ```
   Type: TXT
   Name: @
   Value: (القيمة من Resend)
   ```
5. انتظر التحقق (5-30 دقيقة)

### بدون دومين:
- استخدم البريد الافتراضي من Resend
- سيظهر: `onboarding@resend.dev`
- يعمل بشكل طبيعي للتجربة

---

## 4️⃣ التحقق من الإعداد

### اختبار Cloudinary:
```bash
# افتح المشروع
npm run dev

# اذهب إلى صفحة إضافة منتج
# ارفع صورة
# تحقق من Cloudinary Dashboard
```

### اختبار Resend:
```bash
# سجل مستخدم جديد
# يجب أن يصل بريد ترحيب

# أو اختبر مباشرة:
curl -X POST http://localhost:3000/api/test-email
```

---

## 5️⃣ استكشاف الأخطاء

### Cloudinary لا يعمل:
```bash
# تحقق من:
1. Cloud Name صحيح
2. API Key صحيح
3. API Secret صحيح
4. لا توجد مسافات زائدة
5. أعد تشغيل الخادم
```

### Resend لا يعمل:
```bash
# تحقق من:
1. API Key يبدأ بـ re_
2. API Key صحيح
3. البريد الإلكتروني صحيح
4. تحقق من Resend Logs
```

### خطأ في .env:
```bash
# تأكد من:
1. لا توجد مسافات قبل/بعد =
2. لا توجد علامات اقتباس زائدة
3. الملف اسمه .env وليس .env.txt
4. الملف في المجلد الرئيسي
```

---

## 6️⃣ الحدود المجانية

### Cloudinary:
- ✅ 25 GB تخزين
- ✅ 25 GB bandwidth/شهر
- ✅ كافي لـ 10,000+ صورة

### Resend:
- ✅ 100 بريد/يوم
- ✅ 3,000 بريد/شهر
- ✅ كافي للبداية

### الترقية:
- عند الحاجة فقط
- خطط مدفوعة متاحة
- سهلة الترقية

---

## 7️⃣ نصائح مهمة

### الأمان:
- ❌ لا تشارك API Keys
- ❌ لا ترفع .env على GitHub
- ✅ استخدم .env.example للمشاركة

### الأداء:
- ✅ Cloudinary يحسن الصور تلقائياً
- ✅ CDN سريع عالمياً
- ✅ WebP format للحجم الأصغر

### التكلفة:
- ✅ ابدأ بالمجاني
- ✅ راقب الاستخدام
- ✅ ترقية عند الحاجة فقط

---

## ✅ قائمة التحقق النهائية

```
□ حساب Cloudinary جاهز
□ API Keys في .env
□ حساب Resend جاهز
□ API Key في .env
□ اختبار رفع صورة ✓
□ اختبار إرسال بريد ✓
□ المشروع يعمل بدون أخطاء ✓
```

---

**جاهز للإطلاق! 🚀**
